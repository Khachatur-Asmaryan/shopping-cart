package com.shoppingcart.enums;

/**
 * Enumeration of JWT token types.
 * <p>
 * This enum represents the types of JWT tokens that can be generated and used in the application.
 * It defines two token types: ACCESS_TOKEN and REFRESH_TOKEN.
 */
public enum JwtTokenType {
    ACCESS_TOKEN,

    REFRESH_TOKEN
}
